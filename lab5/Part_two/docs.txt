Разъяснения в теминологии:
1) Шарик (class Ball) - снаряд, который выпускает пушка
2) Мишень (class Target) - собственно говоря, это то, вот что придётся стрелять (мишени все красные)
3) Пушка (class Gun) - пушка из которой стелять
4) Жив или нет - "нужно ли отрисовывать и/или обрабатывать события/обновлять данные для данного экземпляра класса"

Разъяснения по некоторым общим сведениям классов (которые бы хорошо закинуть в большой класс и пихнуть наследование)

При добавлении какого-то дополнительного типа (дополнительная игровая сущность: ну а вдруг)
методы и поля с меткой НЕОБХОДИМ должны быть реализованы
Хотите добавить обработчиков - флаг в руки я не знаю как Вы будете это разгребать
(в методах может быть написано pass, но реализация должна быть; поля должны существовать, как апдейтить, решаейте сами)

- поле is_alive:
    Жив или нет
    НЕОБХОДИМО

- метод update:
    Метод, который обновляет всё состояние объекта на одну итерацию вперед
    Например, везде в данном методе вызывается метод, отвечающий за изменение координат, т.е движение (move)
    Плюс всякие проверки жив или нет, а также что-то дополнительное
    В общем, должно полностью меняеться состояние класса
    НЕОБХОДИМ

- метод draw:
    Метод, отпередляющий как отрисовать экземпляр класса
    НЕОБХОДИМ
    (если нинадо рисовать - pass)

- метод move:
    Изменяет параметры, отчевающие на скорость и координату
    Если подразумевается, что что-то движется, что он необходим в update
    для корректной отрисовки движения того или иного оъекта

Разъяснения по классам
1) Ball
    - функция hittest:
        реализована пока что только для круглых предметов
        конкретно в нашем случае - для 2х видов столкновений
        "шарик - шарик" и "шарик - мишень"
2)